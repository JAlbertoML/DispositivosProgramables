; DESCRIPCIÓN DEL CÓDIGO
    ; SUMAR 5 + 10 (UTILIZANDO VALORES CONSTANTES K)

; DISPOSITIVO A UTILIZAR
    ;LIST P=18F4550
    
; LIBRERÍAS
    #include<xc.inc>
    ;#include <P18F4550.inc>
    

;PALABRA DE CONFIGURACIÓN NO K150
    
    
; ASIGNAR ETIQUETAS A LAS VVARIABLES Y A LOS VALORES CONSTANTES
    DATO_A	EQU	    0X05

; ORIGEN DEL PROGRAMA (DIRECCIÓN DENTRO DE LA MEMORIA DE PROGRAMA PARA ESCRIBIR EN ELLA LA PRIMER INSTRUCCIÓN, POR LO TANTO EL PC TENDRÁ ESE VALOR)
    ;ORIGEN	ORG	    0X00

; CONFIGURAR SFR's
  
; SUBRUTINAS 
    
; PROGRAMA PRINCIPAL
    psect SUMA, local, class = CODE, reloc = 2
    SUMA:
		CLRF	    WREG, 0	; W = 0
		MOVLW	    DATO_A	; W = 5
		ADDLW	    0X0A	; W = 15
		GOTO	    SUMA
		END


